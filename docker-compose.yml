services:
  backend:
    build: ./backend
    env_file: .env
    ports: ["8000:8000"]
    volumes: ["./backend/data:/app/data"]
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]

